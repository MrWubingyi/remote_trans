# 测试配置文件 - 包含一些可能导致问题的配置项

# 正常配置
target_ip=127.0.0.1
target_port=22
listen_port=2222

# 包含格式字符串字符的无效配置键（用于测试安全性）
invalid_key_%s_%d=test_value
another_bad_key_%n=bad_value

# 空行和注释测试

# 无效行（没有等号）
this_is_invalid_line_without_equals

# 正常配置继续
max_clients=5
verbose_logging=1
buffer_size=4096
